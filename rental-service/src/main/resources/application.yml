#app
spring:
  application:
    name: rental-service
server:
  port: 8091
  error:
    include-message: always

#eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
