#app
spring:
  application:
    name: rental-service
  main:
    allow-bean-definition-overriding: true

server:
  port: 8091
  error:
    include-message: always

#eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka

#keycloak
keycloak:
  auth-server-url: http://localhost:8080/auth
  realm: SpringBootKeycloak
  resource: auth-app
  public-client: false
  principal-attribute: preferred_username
  credentials:
    secret: 040afe3d-9d18-4001-965a-f3fbc317b20b
  bearer-only: true
